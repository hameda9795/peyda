# رفع مشکل Header در موبایل

## ✅ مشکلات برطرف شده

### ۱. دکمه‌های هدر روی هم می‌افتادن
**قبل:**
- دکمه "Inloggen" همیشه نشون داده می‌شد
- دکمه "Bedrijf toevoegen" توی موبایل بزرگ (sm) هنوز نشون داده می‌شد

**بعد:**
- "Inloggen" فقط از `md` (768px) به بالا نشون داده می‌شه (`hidden md:block`)
- "Bedrijf toevoegen" فقط از `lg` (1024px) به بالا نشون داده می‌شه (`hidden lg:flex`)

### ۲. محتوای صفحه زیر هدر می‌رفت
**قبل:**
- هدر `fixed` بود (64px ارتفاع)
- Hero section فقط 80px padding-top داشت
- محتوا میرفت زیر هدر

**بعد:**
- Desktop: `padding-top: 7rem` (112px)
- Mobile: `padding-top: 6rem` (96px)
- فاصله کافی برای نمایش محتوا

---

## 📱 وضعیت فعلی Header در دستگاه‌ها

| دستگاه | عرض | دکمه Inloggen | دکمه Bedrijf toevoegen |
|--------|-----|---------------|------------------------|
| iPhone SE | 375px | ❌ مخفی | ❌ مخفی |
| iPhone 15 Pro | 393px | ❌ مخفی | ❌ مخفی |
| iPhone 15 Pro Max | 430px | ❌ مخفی | ❌ مخفی |
| iPad Mini | 768px | ✅ نشان | ❌ مخفی |
| iPad Pro | 1024px | ✅ نشان | ✅ نشان |
| Desktop | 1280px+ | ✅ نشان | ✅ نشان |

---

## 📁 فایل‌های تغییر کرده

| فایل | تغییر |
|------|-------|
| `src/components/layout/Navbar.tsx` | `hidden md:block` و `hidden lg:flex` برای دکمه‌ها |
| `src/app/home.css` | افزایش `padding-top` برای `.atlas-hero` |

---

## 🎯 آیتم‌های هدر در موبایل

توی موبایل (زیر 768px) فقط این آیتم‌ها نشون داده می‌شن:
1. ☰ منوی همبرگر (چپ)
2. **peyda.nl** لوگو (چپ)
3. 🔍 جستجو (راست)
4. 🔔 اعلانات (راست)

این باعث می‌شه هدر تمیز و خلوت باشه و هیچ آیتمی روی هم نیفته.

---

## ⚡ نتیجه

مشکل "یه item رفت روی header" حل شد:
- ✅ دکمه‌ها توی موبایل مخفی شدن
- ✅ فاصله کافی بین هدر و محتوا ایجاد شد
- ✅ هدر تمیز و منظم شد
