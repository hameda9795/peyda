# تغییرات لوگو - peyda.nl

## ✅ تغییرات اعمال شده

### ۱. حذف لوگوی تصویری
- ❌ `public/logo.png` (۱.۴ مگابایت) حذف شد
- ✅ به جای آن متن "peyda.nl" نمایش داده می‌شود

### ۲. فونت Ransom تنظیم شد
- فایل‌های CSS برای فونت Ransom اضافه شد
- Fallback font: Rubik Glitch از Google Fonts
- اگه فونت Ransom آپلود نشده باشه، Rubik Glitch نمایش داده می‌شه

### ۳. رنگ لوگو
- رنگ اصلی: Emerald Green (#059669)
- گرادیان: از #059669 به #34d399
- hover: روشن‌تر می‌شه

### ۴. اندازه Responsive
```
موبایل کوچک (<380px):  1.25rem (20px)
موبایل (<640px):       1.5rem  (24px)
دسکتاپ:               2.5rem  (40px) - با clamp
```

---

## 📁 فایل‌های تغییر کرده

| فایل | تغییر |
|------|-------|
| `src/app/globals.css` | استایل‌های فونت Ransom و لوگو اضافه شد |
| `src/app/layout.tsx` | فونت Rubik Glitch به عنوان fallback اضافه شد |
| `src/components/layout/Navbar.tsx` | `<img>` به `<span class="logo-text">` تغییر کرد |
| `src/components/layout/PremiumFooter.tsx` | لوگوی فوتر با فونت جدید آپدیت شد |
| `src/app/bedrijf-aanmelden/page.tsx` | دو جای لوگو به text تغییر کرد |
| `public/logo.png` | ❌ حذف شد |

---

## 🔧 نحوه آپلود فونت Ransom

### مرحله ۱: فایل‌ها رو آماده کن
فایل‌های فونت Ransom رو پیدا کن (احتمالاً دانلود کردی). باید این فرمت‌ها باشن:
- `Ransom.woff2` (مهم‌ترین)
- `Ransom.woff`
- `Ransom.ttf`
- `Ransom.otf`

### مرحله ۲: کپی به پوشه fonts
فایل‌ها رو بذار توی:
```
public/fonts/
```

### مرحله ۳: تست
سایت رو ری‌استارت کن و ببین لوگو درست نمایش داده می‌شه یا نه.

---

## 🎨 اگه فونت Ransom نداری

### گزینه ۱: از Rubik Glitch استفاده کن (پیش‌فرض فعلی)
همین الان داره کار می‌کنه! Rubik Glitch یه فونت ransom-style از Google Fonts هست.

### گزینه ۲: فونت دیگه از Google Fonts
برو به https://fonts.google.com و یه فونت display پیدا کن (مثل Creepster, Nosifer, etc.)

بعد توی `src/app/layout.tsx` تغییر بده:
```typescript
import { Creepster } from "next/font/google";

const logoFont = Creepster({
    subsets: ["latin"],
    weight: "400",
    variable: "--font-logo",
});
```

و توی `globals.css`:
```css
.logo-text {
  font-family: var(--font-logo), cursive;
}
```

---

## 📱 نمایش در دستگاه‌های مختلف

| دستگاه | اندازه لوگو | رنگ |
|--------|------------|-----|
| موبایل کوچک | ۲۰px | سبز زمردی |
| موبایل | ۲۴px | سبز زمردی |
| تبلت | ۳۲px | سبز زمردی |
| دسکتاپ | ۴۰px | سبز زمردی |
| هدر شفاف | ۴۰px | سفید |

---

## ⚠️ نکات مهم

1. **SEO**: لینک‌های `logo.png` توی فایل‌های JSON-LD schema باقی موندن (برای Google)
2. **fallback**: اگه فونت Ransom لود نشه، Rubik Glitch نمایش داده می‌شه
3. **speed**: حذف لوگوی ۱.۴ مگابایتی = صرفه‌جویی ۱.۵ ثانیه در لود!

---

## 🚀 نتیجه نهایی

قبل:
- ❌ لوگوی ۱.۴ مگابایتی PNG
- ❌ هر بار دانلود می‌شد
- ❌ responsive نبود

بعد:
- ✅ متن "peyda.nl" با فونت Ransom
- ✅ ۰ کیلوبایت (فقط CSS)
- ✅ کاملاً responsive
- ✅ رنگ زیبای سبز زمردی
